{
  "version": "1.6.2",
  "date": "2026-04-13",
  "benchmarks": {
    "simple_insert": {
      "min_ops_per_sec": 1000,
      "description": "Simple INSERT operations"
    },
    "bulk_insert": {
      "min_ops_per_sec": 500,
      "description": "Bulk INSERT in transaction"
    },
    "select_query": {
      "min_ops_per_sec": 500,
      "description": "SELECT queries"
    },
    "update": {
      "min_ops_per_sec": 50,
      "description": "UPDATE operations"
    }
  },
  "notes": "CI regression thresholds. Actual thresholds are in benchmark_validator.zig. Update this file when changing validator thresholds."
}
