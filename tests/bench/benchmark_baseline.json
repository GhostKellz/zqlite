{
  "version": "1.6.2",
  "date": "2026-04-13",
  "benchmarks": {
    "simple_insert": {
      "min_ops_per_sec": 1000,
      "description": "Simple INSERT operations"
    },
    "bulk_insert": {
      "min_ops_per_sec": 500,
      "description": "Bulk INSERT in transaction"
    },
    "select_query": {
      "min_ops_per_sec": 500,
      "description": "SELECT queries"
    },
    "update": {
      "min_ops_per_sec": 50,
      "description": "UPDATE operations"
    }
  },
  "notes": "Documented benchmark thresholds. The executable source of truth is the benchmarks array in benchmark_validator.zig; it uses monotonic timing, warmups, multiple samples, and median/p95 reporting."
}
