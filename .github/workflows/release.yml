name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release-linux:
    name: Build Linux Release
    runs-on: self-hosted
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Clean workspace
        run: rm -rf zig-out

      - name: Zig version
        run: zig version

      - name: Run release validation
        run: ./scripts/test-release.sh

      - name: Build Linux release artifacts
        run: zig build -Dprofile=advanced -Doptimize=ReleaseFast

      - name: Create Linux archive
        run: |
          set -euo pipefail
          VERSION="${{ github.ref_name }}"
          TARGET="x86_64-unknown-linux-gnu"
          ARCHIVE_DIR="zqlite-${VERSION}-${TARGET}"

          mkdir -p "$ARCHIVE_DIR/bin" "$ARCHIVE_DIR/lib" "$ARCHIVE_DIR/include"
          cp zig-out/bin/zqlite "$ARCHIVE_DIR/bin/"
          cp zig-out/lib/libzqlite.a "$ARCHIVE_DIR/lib/"
          cp zig-out/lib/libzqlite_c.a "$ARCHIVE_DIR/lib/"
          cp zig-out/lib/libzqlite_c.so "$ARCHIVE_DIR/lib/"
          cp zig-out/include/zqlite.h "$ARCHIVE_DIR/include/"
          cp README.md CHANGELOG.md LICENSE SECURITY.md install.sh build.zig build.zig.zon "$ARCHIVE_DIR/"
          cp -R src docs examples tests scripts "$ARCHIVE_DIR/"

          tar -czvf "zqlite-${VERSION}-${TARGET}.tar.gz" "$ARCHIVE_DIR"

      - name: Generate SHA256 checksum
        run: |
          set -euo pipefail
          VERSION="${{ github.ref_name }}"
          TARGET="x86_64-unknown-linux-gnu"
          sha256sum "zqlite-${VERSION}-${TARGET}.tar.gz" > "zqlite-${VERSION}-${TARGET}.tar.gz.sha256"

      - name: Upload release artifacts
        uses: actions/upload-artifact@v7
        with:
          name: linux-release
          path: |
            zqlite-${{ github.ref_name }}-x86_64-unknown-linux-gnu.tar.gz
            zqlite-${{ github.ref_name }}-x86_64-unknown-linux-gnu.tar.gz.sha256

  create-release:
    name: Create GitHub Release
    needs: [release-linux]
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Download release artifacts
        uses: actions/download-artifact@v8
        with:
          path: release-artifacts

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          files: |
            release-artifacts/linux-release/*
          draft: false
          prerelease: ${{ contains(github.ref_name, 'rc') || contains(github.ref_name, 'beta') || contains(github.ref_name, 'alpha') }}
          generate_release_notes: true
          body: |
            ## ZQLite ${{ github.ref_name }}

            ### Downloads

            | Platform | File | Checksum |
            |----------|------|----------|
            | Linux (x86_64) | `zqlite-${{ github.ref_name }}-x86_64-unknown-linux-gnu.tar.gz` | `.sha256` |

            ### Quick Install

            ```bash
            curl -fsSL https://raw.githubusercontent.com/ghostkellz/zqlite/refs/heads/main/install.sh -o install.sh
            less install.sh
            bash install.sh
            ```

            ### Manual Install

            ```bash
            tar -xzf zqlite-${{ github.ref_name }}-x86_64-unknown-linux-gnu.tar.gz
            cp zqlite-${{ github.ref_name }}-x86_64-unknown-linux-gnu/bin/zqlite ~/.local/bin/
            zqlite --version
            ```

            ### Verify Checksum

            ```bash
            sha256sum -c zqlite-${{ github.ref_name }}-x86_64-unknown-linux-gnu.tar.gz.sha256
            ```
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
